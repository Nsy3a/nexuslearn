# NexusLearn 即时实施计划（本周开始）

## 🎯 本周重点任务（立即执行）

### Day 1-2: 环境检查与基础优化
1. **服务状态检查**
   ```bash
   # 检查后端服务（当前在terminal 6运行）
   curl http://localhost:8081/health
   
   # 检查前端服务
   cd nexuslearn-frontend && npm run dev
   ```

2. **API统一性验证**
   - 确认所有前端调用使用`apiFetchJson`统一接口
   - 验证端口混乱问题已解决（8081统一）
   - 测试跨域配置正确性

3. **基础数据准备**
   - 创建测试用户数据
   - 准备测试用学习目标和计划
   - 设置测试用的社区圈子数据

### Day 3-4: ContextEngine真实数据接入

#### 前端改造（nexuslearn-frontend/src/components/SmartContextSidebar.vue）
```typescript
// 新增API调用方法
async function loadUserProfile() {
  const profile = await apiFetchJson(`/profile/${userId}`)
  userProfile.value = profile
}

async function loadInteractionHistory() {
  const history = await apiFetchJson(`/history/${userId}`)
  interactionHistory.value = history
}

async function updatePreferences(newPrefs: UserPreferences) {
  await apiFetchJson(`/profile/${userId}`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(newPrefs)
  })
}
```

#### 后端验证（nexuslearn/backend/server.js）
- 确认`/profile/:id`和`/history/:id`接口数据格式
- 验证偏好设置对搜索和学习计划的影响
- 测试历史记录更新机制

### Day 5-7: Community.vue生态对接

#### 朋友圈功能实现
```typescript
// 获取朋友动态
async function loadFriendsPosts() {
  const posts = await apiFetchJson(`/friends/posts?userId=${userId}`)
  friendsPosts.value = posts
}

// 发布动态
async function publishPost(content: string) {
  await apiFetchJson('/friends/posts', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ userId, content })
  })
}
```

#### 兴趣圈功能增强
- 实现圈子加入/退出功能
- 添加圈子内发帖和讨论
- 集成治理提案功能

## 🚀 下周详细计划

### Week 2: AI帮问系统打通

#### 后端AI服务集成
1. **AI分解服务对接**
   ```javascript
   // 在server.js中确认已有接口
   app.post('/ai/decompose', async (req, res) => {
     const { userId, requirement } = req.body
     // 调用AI处理器进行需求分解
     const decomposition = await aiProcessor.decompose(requirement)
     res.json(decomposition)
   })
   ```

2. **社区任务生成**
   - 将AI分解结果转为圈子任务
   - 实现任务悬赏和分配机制
   - 添加任务进度跟踪

#### 前端AI帮问界面
```vue
<!-- 新增AI帮问组件 -->
<template>
  <div class="ai-help-request">
    <textarea v-model="requirement" placeholder="描述你的需求..." />
    <button @click="submitToAI">AI帮问</button>
    <div v-if="decomposition" class="ai-suggestions">
      <h3>AI建议的子任务：</h3>
      <ul>
        <li v-for="task in decomposition.tasks" :key="task.id">
          {{ task.description }}
        </li>
      </ul>
    </div>
  </div>
</template>
```

### Week 3: 搜索与偏好深度融合

#### 搜索算法优化
1. **偏好权重调整**
   - 实现60%偏好匹配 + 40%质量分算法
   - 添加用户反馈循环优化
   - 支持多维度筛选组合

2. **语义搜索增强**
   - 集成向量数据库查询
   - 实现同义词扩展
   - 添加搜索意图识别

#### 个性化推荐系统
```typescript
// 基于用户画像的推荐算法
function calculateRecommendationScore(item: any, userProfile: UserProfile): number {
  const preferenceScore = calculatePreferenceMatch(item, userProfile) * 0.6
  const qualityScore = item.qualityScore * 0.4
  return preferenceScore + qualityScore
}
```

## 📊 月度目标与验收标准

### Month 1 目标：三向闭环打通
- [ ] 用户→系统→AI→用户 完整闭环
- [ ] ContextEngine驱动所有功能
- [ ] 社区活跃度提升50%
- [ ] 搜索准确率提升30%

### 验收测试清单
```bash
# 1. 学习闭环测试
curl -X POST http://localhost:8081/goals \
  -H "Content-Type: application/json" \
  -d '{"userId":"test1","title":"Learn Vue3","target":"3 months"}'

# 2. AI帮问测试  
curl -X POST http://localhost:8081/ai/decompose \
  -H "Content-Type: application/json" \
  -d '{"userId":"test1","requirement":"Build a blog system"}'

# 3. 社区功能测试
curl -X POST http://localhost:8081/circles \
  -H "Content-Type: application/json" \
  -d '{"name":"Vue Developers","description":"Vue.js learning group"}'

# 4. 搜索偏好测试
curl "http://localhost:8081/search?q=vue&userId=test1&filters={\"level\":\"intermediate\"}"
```

## 🔧 技术债务清理

### 高优先级
1. **错误处理统一**
   - 统一错误码规范
   - 前端错误友好提示
   - 后端错误日志规范

2. **性能优化**
   - API响应时间监控
   - 数据库查询优化
   - 前端bundle大小优化

3. **安全加固**
   - JWT token刷新机制
   - API速率限制细化
   - 输入验证强化

### 中优先级
1. **代码质量**
   - TypeScript类型完善
   - 单元测试覆盖率提升
   - 代码注释标准化

2. **文档完善**
   - API文档更新
   - 部署文档细化
   - 用户指南编写

## 📈 成功指标跟踪

### 每日监控
- API响应时间（目标<500ms）
- 错误率（目标<1%）
- 活跃用户数
- 新功能使用频率

### 每周评估
- 用户反馈收集
- 性能瓶颈分析
- 功能使用统计
- 社区增长情况

### 每月总结
- 目标达成情况
- 技术债务清理进度
- 用户满意度调研
- 下月计划调整

## ⚡ 应急响应计划

### 常见问题快速修复
1. **服务不可用**
   ```bash
   # 检查服务状态
   pm2 status
   # 重启服务
   pm2 restart nexuslearn-backend
   ```

2. **数据库连接问题**
   ```bash
   # 检查PostgreSQL
   docker-compose ps
   # 重启数据库
   docker-compose restart postgres
   ```

3. **前端构建失败**
   ```bash
   # 清理缓存
   rm -rf node_modules package-lock.json
   npm install
   npm run build
   ```

### 升级回滚策略
- 数据库migration备份
- 代码版本tag管理
- 快速切换脚本准备
- 用户通知机制

---

## 🎯 本周立即行动项

### 今天开始（优先级最高）
1. ✅ 检查当前服务运行状态
2. 🔄 验证API统一性（预计2小时）
3. 📊 准备测试数据集（预计1小时）
4. 🔧 开始ContextEngine改造（预计1天）

### 本周必须完成
1. ContextEngine真实数据接入
2. Community.vue基础功能对接
3. 测试环境数据准备
4. 基础性能验证

> 🚀 **目标**：本周完成基础闭环验证，为下周AI帮问系统打通做好准备！

**准备好了吗？让我们开始执行！** 💪