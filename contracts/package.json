{
  "name": "contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia && hardhat run scripts/deploy_governance.js --network sepolia && node scripts/write_env.js sepolia",
    "deploy:amoy": "hardhat run scripts/deploy.js --network polygonAmoy && hardhat run scripts/deploy_governance.js --network polygonAmoy && node scripts/write_env.js polygonAmoy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^4.9.5",
    "dotenv": "^16.6.1",
    "hardhat": "^2.22.17"
  }
}
